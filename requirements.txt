# Core UI
pywebview>=5.0

# Cross-platform data directory resolution
platformdirs>=4.0

# ASR — Qwen3 (primary engine, CPU/CUDA)
qwen-asr>=0.0.6
torch

# ASR — Whisper (install ONE depending on platform)
# Apple Silicon (macOS arm64):  pip install mlx-whisper
# Other (Linux/Windows/x86):   pip install faster-whisper

# Speaker diarization
pyannote.audio>=4.0

# Audio processing
# ffmpeg and ffprobe must be installed separately:
#   macOS:   brew install ffmpeg
#   Linux:   sudo apt install ffmpeg
#   Windows: https://ffmpeg.org/download.html (add to PATH)

# Model downloading
huggingface-hub>=0.23

# LLM summary (OpenAI-compatible API)
openai>=1.0

# Realtime transcription
sounddevice>=0.4      # cross-platform microphone input
silero-vad>=4.0       # voice activity detection (PyTorch-based)
numpy>=1.24           # audio array processing (transitive dep of torch, listed explicitly)

# Frontend testing (Playwright)
# After pip install, also run: playwright install chromium
playwright>=1.40
pytest-playwright>=0.4
